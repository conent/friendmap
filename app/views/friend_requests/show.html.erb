<p id="notice"><%= notice %></p>



<p>
  <b>Otherfriend:</b>
  <%= @friend_request.friend_id %>
</p>
<table class="table table-striped">
  <tr>
    <th>Asked by</th>
    <th>Email address</th>
    <th>When</th>
    <th></th>
  </tr>
  <tr>
    <td><%= Friend.find_by_id(@friend_request.friend_id).name %> <%= Friend.find_by_id(@friend_request.friend_id).surname %></td>
    <td><%= Friend.find_by_id(@friend_request.friend_id).email %></td>
    <td><%= @friend_request.created_at %></td>
  </tr>

</table>


<%= link_to 'Back', friend_requests_path %>
